# Copier en .env.local et adapter. Toutes ces variables sont exposées au
# navigateur (préfixe NEXT_PUBLIC_) : n'y mettre aucun secret.

# API ARGOS (apps/api)
NEXT_PUBLIC_API_URL=http://127.0.0.1:3005

# Moteur d'itinéraire (voir docs/adr/0001-moteur-de-routage.md)
#   valhalla = auto-hébergé (infra/compose) — OBLIGATOIRE en production
#   osrm     = service compatible OSRM ; par défaut la démo publique (dev seulement)
NEXT_PUBLIC_ROUTING_ENGINE=valhalla
NEXT_PUBLIC_ROUTING_URL=http://localhost:8002
